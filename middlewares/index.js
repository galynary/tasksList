const validateBody = require("./validateBody");
const isValidId = require("./isValidId");
const authenticate = require("./authenticare");
const upload = require("./upload.js");

module.exports = {
	validateBody,
	isValidId,
	authenticate,
	upload,
};
